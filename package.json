{
  "name": "uopca-courses",
  "version": "1.0.0",
  "main": "dist/main/main.js",
  "scripts": {
    "start": "electron .",
    "build": "tsc && npm run copy-files",
    "copy-files": "cp src/renderer/index.html dist/renderer/ && cp -r courses dist/",
    "dev": "npm run build && npm start"
  },
  "dependencies": {
    "@types/axios": "^0.14.4",
    "axios": "^1.7.7",
    "electron": "^22.0.0",
    "electron-store": "^8.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.9.4",
    "typescript": "^5.3.2"
  }
}
